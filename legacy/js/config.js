export const config = window.tripConfig;

export const dayColorPalette = config.dayColors;

export const defaultTripSettings = config.tripSettings;

export const defaultWishes = config.defaultWishes;
